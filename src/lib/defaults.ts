export const DEFAULT_GUESTS = 0;
export const DEFAULT_BUDGET_PER_PERSON = 0;
export const DEFAULT_TOTAL_BUDGET = 0;

export const DEFAULT_SOCIAL_PROOF_STATS = [
  { stat: "0", label: "Schulen und Klassen" },
  { stat: "0", label: "Glückliche Gäste" }
] as const;
