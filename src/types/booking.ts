export type BookingRequest = {
  id: string;
  locationId: string;
  locationName: string;
  name: string;
  email: string;
  schoolName: string;
  guests: number;
  preferredDate: string;
  createdAt: string;
};
